@page "/TractMain"
@page "/TractMain/{IdFromListView}"

@using BlazorWebApp.Components.Pages.OG_Forms.Properties.Tracts.TractTabs.SubTabInternals.TractMorePage


@inherits TractMainPageBase


@{
@* page "/Tracts/{EntityID}/{IdFromListView}" *@

    bool UserSelectedNewTract = false;
    bool UserSelectedViewTractsList = false;

    async Task UserCreatesNewTract()
    {
        if (UserSelectedViewTractsList == true)
        {
            UserSelectedViewTractsList = false;
        }

        UserSelectedNewTract = true;
    }

    async Task UserViewsExistingTracts()
    {
        if (UserSelectedNewTract == true)
        {
            UserSelectedNewTract = false;
        }

        UserSelectedViewTractsList = true;
    }

}

<br />

<CreateOrUpdateTractComponent 
    InitialState="@InitialState"
    EntityID="@EntityID"
    IdFromListView="@IdFromListView"
        >
</CreateOrUpdateTractComponent>


@code {


    private bool IsFirstCheckboxChecked { get; set; } = false;
    private bool IsSecondCheckboxChecked { get; set; } = false;

    protected override async Task OnInitializedAsync()
    {
        // if (InitialState == true)
        // {
        //     TractModel.TractId = TractID;
        // }
        // else
        // if (!string.IsNullOrEmpty(TractID))
        // {
        //     InitialState = false;
        // }
    }

    // private readonly Dictionary<string, Type> components = new Dictionary<string, Type>()
    // {
    //         ["Associated Leases"] = typeof(ConnectedTractLeases)
    //     //,
    //     // ["SpaceX"] = typeof(SpaceX),
    //     // ["ULA"] = typeof(UnitedLaunchAlliance),
    //     // ["Virgin Galactic"] = typeof(VirginGalactic)
    // };
    // private Type? selectedType;

    // private void OnDropdownChange(ChangeEventArgs e)
    // {
    //     if ((e.Value is string dropdownValue) && !String.IsNullOrWhiteSpace(dropdownValue))
    //     {
    //         selectedType = components[dropdownValue];
    //     }
    //     else
    //     {
    //         selectedType = null;
    //     }
    // }
}